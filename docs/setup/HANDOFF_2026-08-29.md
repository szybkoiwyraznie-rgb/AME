# HANDOFF — sesja 2026-08-29/30 (materializacja 5 kart MtG)

> Skrót JEDNEJ sesji. Zasady: `AGENTS.md`, `docs/PROTOKOL.md` (v1.7), ADR 0001–0021.
> Poprzednie: `HANDOFF_2026-08-28.md`, `-M5.md`…`-M9.md`.

## Kontekst

Właściciel zlecił **pełną materializację pięciu kart** do Manifestacji
Eterycznych: Apprentice Wizard (2XM #40), Panic Spellbomb (SOM #191),
Trade Route Envoy (TDM #163), Akroan Sergeant (ORI #130), Infectious Horror
(CON #47). Każda karta = pełny wpis wg PROTOKÓŁ v1.7, dane wyłącznie ze
Scryfall (ADR 0008), tagi tylko z kanonu, `meta.modyfikacje` z godziną,
`data/index.json` wyłącznie z `npm run build`, nowa gałąź + PR przed kodowaniem.

## Gałąź i PR

- Gałąź **`mtg/materializacja-5-kart`** (baza: `origin/main` = 101c75d),
  PR **#11** otwarty przed kodowaniem (commit startowy `de71db7`).
- PR #10 (Pętla Jakości z poprzedniej sesji, na `arena/01a04f70-ame`)
  **pozostał nietknięty** — 6 commitów, OPEN, MERGEABLE, do decyzji właściciela.

## Co zrobiono

1. **Fetch Scryfall** — 5 stron kart: dokładne Oracle, flavor, artyści,
   znak wodny/reguły, printy, legalności (Apprentice Wizard ma niemiecki
   tytuł „Zauberlehrling”; Panic Spellbomb i Akroan Sergeant **nie mają**
   flavoru; Infectious Horror ma ruling 2HG −4 życia).
2. **Research źródeł** (wszystko pobrane i zweryfikowane):
   - Lukian „Filopseudes” (Fowler 1905, pełny tekst), Goethe „Der
     Zauberlehrling” 1798 (Wikisource), Wikipedia de (Wieland/Lucian),
     typ baśniowy ATU 325* (Exiw);
   - Hezjod „Prace i dnie” 42–105 (Evelyn-White), Theoi Pandora, Britannica;
   - Rigweda 10.14 (Griffith), Wikipedia „Sharvara and Shyama” (Mithūdṛśā,
     Canis Major/Minor, Atharweda), legenda Manikarniki (waranasi);
   - Herodot 7.104 (Demaratos: „prawo, którego boją się bardziej…”),
     LSJ G4414 (πρωτοστάτης), Plutarch, „Wypowiedzi kobiet spartańskich”
     („albo z tym, albo na tym”);
   - Morowa Panna: lasmgiel.pl, naszeszlaki.pl (Chojnice), artykuł KUL
     („Zaraza nadchodzi i odchodzi”), Wójcicki (demony moru, Marzanna).
   - Grettis saga/draugr badane w toku sesji — **nie użyte** (Infectious
     Horror dostał morową pannę; draugr pozostaje kandydatem na inną kartę).
3. **Pięć wpisów**: `knecht-z-koptos`, `pandora`, `syama-i-sarvara`,
   `protostates`, `morowa-panna` — sekcje I–V, `obraz: null`, tagi z kanonu
   (kultura/typ/motyw/postac), powiązania 3× (w tym między nowymi bytami).
4. **Kanon**: nowy tag kulturowy **`polska`** (44 tagi; 11 kultur).
5. **Weryfikacja**: `npm run build` + `npm test` **163/163** + `npm run check`
   zielone; snapshoty `test/kronika.test.js` i `test/dane.test.js`
   zaktualizowane (paliwo pasywne i oś epok zależą od sieci powiązań —
   nowe wartości wyliczone narzędziem, nie ręcznie).
   Commit `9aecf1a`, wypchnięty.

## Stan na koniec

- `data/manifestations/`: **20 wpisów** (15 + 5 nowych);
- `data/skity/`: **18** (bez zmian — SKITy nie były zlecone);
- `data/kanon-tagow.json`: **44 tagi** (10 → 11 kultur);
- `data/index.json` i Kronika: wygenerowane przez build (20 bytów,
  seedy 35/65; epoki Tomu I przeliczone na nowym paliwie pasywnym);
- Gałąź `mtg/materializacja-5-kart`, PR #11 OPEN; brama zielona.

## Otwarte / następna sesja

- **PR #11** czeka na decyzję właściciela (squash and merge kończy sesję).
- **PR #10** nadal otwarty — właściciel decyduje o kolejności scalania;
  oba PR-y są na różnych gałęziach (brak konfliktów treściowych — oba
  modyfikują `data/index.json` i `docs/kronika-*.html`, więc drugi z nich
  po sceleniu pierwszego odzyska własny stan po `npm run build`).
- Defekty otwarte (niezależne): `replace_topbar.cjs` niepodpięty;
  `rebuild-index.mjs --check` bez try/catch na JSON.parse; `dane.test.js`
  re-używa buildTime; typo selkie→nessos we wpisie (do poprawy przy C1).
- C1 (pogłębianie) w kolejce: ben-varrey, kannon-hase, talos-kreta
  (po 4 źródła) — przesunięta przez to zlecenie.
- Wizerunki nowych bytów: `obraz: null` — do uzupełnienia w przyszłości
  (assets/wizualizacje/<slug>.jpg, 21:9, ≤2 MB).
