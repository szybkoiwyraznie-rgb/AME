# HANDOFF — sesja 2026-08-29 (audyt PR #8/#9 + Pętla Jakości C1→C3→C2)

> Skrót JEDNEJ sesji. Zasady: `AGENTS.md`, `docs/PROTOKOL.md` (v1.7), ADR 0001–0021.
> Poprzednie: `HANDOFF_2026-08-28-M13.md` …; ta sesja: gałąź `arena/01a04f70-ame`,
> PR #10 (otwarty; scalenie — decyzja właściciela).

## Kontekst

Właściciel: „Kontynuujemy projekt.” Brak zlecenia treściowego → po audycie
Pętla Jakości (ADR 0007). Baza na starcie: `npm test` 163/163, build/check OK,
indeks 15 wpisów / 18 skitów / 43 tagi / 5 epok Kroniki.

## Co zrobiono

1. **Audyt B (PR #9 + rzut oka na PR #8)** — oba scalone 2026-08-29 bez
   handoffu. Wyniki w `docs/PROJECT_HISTORY.md` i opisie PR #10.
   Znalezione: (a) **martwe linki** z paska Kroniki (`?q=`, `?action=…` —
   aplikacja czytała tylko hash); (b) **brak `meta.modyfikacje`** w PR #8
   (17 przeredagowanych skitów + 14 wpisów — feed ich nie pokazywał);
   (c) nieaktualny komentarz stopki w `app/ui.js`.
2. **Naprawa audytowa** (commit `076a0d9`): backfill `meta` w 31 plikach
   (data 2026-08-29 21:36, opisy czytelne), komentarz, `docs/PROJECT_HISTORY.md`.
3. **C1** (commit `dfd0466`): `agni` 3→6 źródeł (Wikipedia, Rigweda 1.1
   Griffith, Agni Purana; trzy poziomy obecności, sapta jihvā, saptapadi);
   `sfinks-teby` 3→4 (Pauzaniasz 9.26.2 na Perseusie — wariant córki Lajosa /
   piratki z Anthedonu; zagadka od Muz, Apollodor 3.5.8).
4. **C3** (commit `1096f20`): SKIT „DZIEŃ WOLNEGO” (`data/skity/dzien-wolnego.json`,
   269 słów) — Agni × Ben-Varrey × Kannon, unikalny skład, rejestr lekki; trzy
   byty bez skitu dostały debiut. Skity: 18 → **19**.
5. **C2** (commit `716f820`): `?q=`, `?action=wylosuj`, `?action=powiazania`
   w aplikacji głównej (`akcjeZZapytania` w `app/ui.js` + 7 testów + wpięcie
   w `start()`); `aria-pressed` przycisku motywu w `tools/kronika.mjs`.
   Zweryfikowane headless Chromium (filtr, powiązania, losowanie działają
   z paska Kroniki). Lekcja L16.

## Stan na koniec

- `npm test` **164/164**, `npm run build` + `npm run check` zielone.
- Indeks: 15 wpisów, 19 skitów, 43 tagi; feed z pozycjami z 2026-08-29.
- PR #10 ma 5 commitów (startowy, audyt, C1, C3, C2) + dokumentacja w tej
  sesji (ROADMAP/LESSONS/README/PROJECT_HISTORY/handoff).
- BuildTime w indeksie: generowany (PR #9) — `npm run build` zachowuje stary
  czas, gdy treść się nie zmienia.

## Co się zmieniło (pliki, względem PR #9)

| Plik | Zmiana |
|---|---|
| `data/manifestations/agni.json`, `sfinks-teby.json` | C1: +4 źródła, nowe treści, meta |
| `data/skity/dzien-wolnego.json` | **nowy SKIT** (C3) |
| `app/ui.js` | `akcjeZZapytania` + komentarz stopki |
| `app/app.js` | wpięcie akcji z zapytania w `start()` |
| `tools/kronika.mjs` | `aria-pressed` przycisku motywu |
| `test/ui.test.js` | +7 asercji (`akcjeZZapytania`) |
| `docs/PROJECT_HISTORY.md`, `ROADMAP.md`, `LESSONS.md` (L16), `README.md` | opis sesji / L16 |
| 14 wpisów + 17 skitów | backfill `meta.modyfikacje` (audyt) |

## Rzeczy otwarte / kolejka

1. **Scalenie PR #10** — decyzja właściciela (preferowane squash).
2. **Kolejne C1** — kandydaci z najmniejszą liczbą źródeł: `ben-varrey` (4),
   `sfinks-teby` (4, zrobione), `agni` (6, zrobione), `kannon-hase` (4);
   cel: ~6 źródeł na wpis (M13).
3. **C3** — skity dla bytów bez udziału: zostały `talos-kreta` (1 skit)…
   właściwie wszystkie mają ≥1; nowe składy: agni/ben-varrey/kannon mają
   teraz 1 — kolejne tria np. z `taros`/... dobierać z `podpowiedzSkladySkitow`.
4. **X** — `replace_topbar.cjs` (jednorazowy patch z PR #9): kandydat do
   przeniesienia do `tools/` albo usunięcia przy okazji (niepodpięty do npm).
5. **Kronika**: Epoka V istnieje; następna epoka może wprowadzić nowych
   uczestników (seed obejmuje wszystkie 15 bytów). Mechanizm bez zmian.
