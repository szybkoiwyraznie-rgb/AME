# HANDOFF — sesja 2026-08-29/30 (Pętla Jakości + materializacja 5 kart)

> Skrót jednej sesji Areny z dwoma zleceniami, połączonych w jeden PR #10
> (gałąź `arena/01a04f70-ame`). Zasady: `AGENTS.md`, `docs/PROTOKOL.md`
> (v1.7), ADR 0001–0021. Poprzednie: `HANDOFF_2026-08-28-M13.md`, … .

## Kontekst

1. **Zlecenie 1:** „Kontynuujemy projekt.” Brak zlecenia treściowego → audyt
   i Pętla Jakości (ADR 0007).
2. **Zlecenie 2:** pełna materializacja pięciu kart MtG do Manifestacji
   Eterycznych (dane wyłącznie ze Scryfall, tagi tylko z kanonu, indeks
   wyłącznie z `npm run build`, PR przed kodowaniem).

Pracę 2 rozpoczęto na osobnej gałęzi `mtg/materializacja-5-kart` (PR #11),
lecz na polecenie właściciela **2026-08-30 przeniesiono ją do PR #10** —
jeden PR = ciągłość dostępu do GitHub i odtworzenia po resecie sandboxa.

## Gałąź i PR

- **Gałąź:** `arena/01a04f70-ame` (baza: `origin/main` 101c75d po PR #9).
- **PR #10** — otwarty, scalenie decyzją właściciela. **12 commitów:**
  `c6bfde4` (start), `076a0d9` (audyt + backfill), `dfd0466` (C1),
  `1096f20` (C3), `716f820` (C2), `264c0e6` (dokumentacja), `fe68821`
  (start materializacji), `a72b45a` (5 wpisów + kanon), `b620a50` (dziennik),
  `81f910c` (wizualizacje), `d4bbada` (przebudowa po połączeniu),
  `9xxxxxx` (docs: §5 + handoff).
- **PR #11** — zamknięty bez scalenia, gałąź `mtg/materializacja-5-kart`
  usunięta; jej cztery commity zachowane w historii PR #10.

## Co zrobiono

### Część 1 — audyt + Pętla Jakości (C1→C3→C2)

- **Audyt PR #8/#9:** (a) martwe linki z paska Kroniki (`?q=`, `?action=…` —
  aplikacja czytała tylko hash); (b) brak `meta.modyfikacje` w PR #8
  (17 skitów + 14 wpisów — feed ich nie pokazywał). Wyniki w
  `PROJECT_HISTORY.md` i opisie PR #10.
- **Backfill:** `meta.modyfikacje` dla ~50 plików + komentarz w stopce
  („co nowego”).
- **C1:** pogłębienie `agni` i `sfinks-teby` (research www, +4 źródła,
  wariant Pausaniasza).
- **C3:** SKIT „Dzień wolnego” (Agni × Ben-Varrey × Kannon, unikalny skład,
  rejestr lekki).
- **C2:** `?q=` (filtr manifestacji) i `?action=wylosuj|powiazania`
  w aplikacji głównej — naprawa linków z Kroniki (PR #9); test
  headless Chromium L16.
- **Dokumentacja:** ROADMAP, LESSONS L16, README, PROJECT_HISTORY.

### Część 2 — materializacja 5 kart MtG

| Karta | Byt | Plik / tagi |
|---|---|---|
| Apprentice Wizard (2XM 40) | Knecht z Koptos — ożywiony tłuczek | `knecht-z-koptos.json` — grecja, automat, warunek, przemiana |
| Panic Spellbomb (SOM 191) | Pandora — pithos, plagi, Elpis | `pandora.json` — grecja, automat, warunek, zagadka |
| Trade Route Envoy (TDM 163) | Śyāma i Śarvara — czterookie psy Yamy | `syama-i-sarvara.json` — indie, fantom, przewodnik, obrona, postac-zwierzeca |
| Akroan Sergeant (ORI 130) | Protostates — fantom pierwszego szeregu | `protostates.json` — grecja, fantom, furia, pamiec |
| Infectious Horror (CON 47) | Morowa Panna — słowiańska zaraza | `morowa-panna.json` — polska, fantom, warunek, zagadka |

- Każdy wpis: sekcje I–V wg PROTOKÓŁ v1.7, `meta.modyfikacje`
  `2026-08-30 00:09` (aktywność) i `00:20` (wizualizacje), `dokumentacja[]`
  ze Scryfall + źródłami www (RV 10.14, Hdt. 7.104, LSJ G4414, lasmgiel.pl,
  naszeszlaki.pl, KUL, UWM).
- **Kanon:** nowy tag `polska` (folklor słowiański, personifikacje zarazy)
  → 44 tagi / 11 kultur, w tym samym commicie co wpisy.
- **Wizualizacje 21:9:** 5 obrazów wygenerowanych z ramowych promptów,
  `assets/wizualizacje/<slug>.jpg` (1915×821 = 21:9, 209–276 KB ≤ 2 MB),
  pola `wizualizacja.obraz` podpięte; **wszystkie 20 wpisów ma obraz**.
- **PROTOKÓŁ §5:** usunięty zdanie o „miejscu złożenia / wizualizacja
  nieodtworzona” — dotyczyło innego mechanizmu i nie było potrzebne
  (polecenie właściciela).


### Część 3 — redakcja SKITów (B) i audyt Kartoteki (C)

- **B:** przeczytane wszystkie 19 SKITów; 17 poprawionych (jeden commit
  `37924bd`), 2 bez zmian (`cena-znaku`, `dzien-wolnego`). Poprawki
  językowe, logiczne i humor; kluczowe ustalenie: **Selkie z Sule Skerry
  jest męski** (ojciec z ballady) — formy męskie przywrócone w
  `kalendarz-powrotow` i `znak-i-liczba` (commit `c193fc1`).
- **C:** przeczytane wszystkie 20 Kart; 16 poprawionych (`c193fc1`),
  4 bez uwag (empusa, kannon, morowa-panna, nessos + drobne w innych).
  M.in. „Nielubi”→„Nie lubi”, „słaja”→„słoja”, „Pauzaniasz”, „Lajos”,
  „czarną kitą”, „Imię bytu”, spójność powiązań z mitem.
- Każda zmiana ma `meta.modyfikacje` z datą i opisem; słowa SKITów ≤ 300.

## Stan

- **Brama:** `npm test` **164/164**, `npm run build` i `npm run check` zielone.
- **Indeks:** 20 wpisów / **19 skitów** / 44 tagi (11 kultur) / 101 pozycji
  „Co nowego”; 5 epok Kroniki; buildTime
  `2026-08-29T22:28:04.077Z`; osie Tomu I przeliczone ze związkami.
- Testy Kroniki i danych mają snapshoty dopasowane do rozszerzonej sieci
  (kultura 11; paliwo agni 16, egungun 20, kentaur-pelion 18, empusa-korynt 19,
  lincoln-imp 12, sfinks-teby 22, talos-kreta 18).

## Otwarte / kolejka

- **Decyzja właściciela:** scalenie PR #10 (squash and merge) — potem
  `npm run build` w nowej sesji z `main`.
- **Kolejka C1 (po scaleniu):** ben-varrey, kannon-hase, talos-kreta.
- **Defekty otwarte:** `replace_topbar.cjs` niepodpięty;
  `rebuild-index.mjs --check` bez try/catch na JSON.parse;
  `dane.test.js` reużywa buildTime; typo selkie→nessos we wpisie.
- Nowe pomysły → `docs/BACKLOG.md`.
