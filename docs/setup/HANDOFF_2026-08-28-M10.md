# HANDOFF — sesja 2026-08-28 M10 (koncepcja SPLOT/KRONIKA + treść)

> Skrót JEDNEJ sesji. Zasady: `AGENTS.md`, `docs/PROTOKOL.md` (v1.7), ADR 0001–0019.
> Poprzednie: `HANDOFF_2026-08-28-M9.md` … `-M3.md`, `HANDOFF_2026-08-28.md` (M2),
> `HANDOFF_2026-08-27.md` (M1).

## Kontekst

Właściciel odpowiedział na 3 pytania z M9 o grywalizację: A i B oba warte
rozważenia; **B ożywa dzięki subagentom wcielonym w pojedyncze byty** (dossier:
percepcja, wiedza, wierzenia, ograniczenia — nie jeden wszechwiedzący narrator);
dwie architektury AI (runtime z kluczami w sesji albo pregenerowane popychane
przez agentów Areny); **na razie myślimy, nie kodujemy gry — w międzyczasie
treść**. Praca dołożona do PR #6.

## Co zrobiono

- **Główny rezultat (namysł):** `docs/plans/POMYSL_splot-i-kronika-koncepcja.md`
  — A i B jako dwie warstwy jednej gry: SPLOT (świat, który się zmienia) +
  KRONIKA (wcielone spotkania przez subagentów-postacie z dossier). Metodyka
  dossier i pętli spotkania, model danych `data/kronika/*.json`, rekomendacja
  architektury **3b** (rozgrywka jako dane popychane przez kolejnych agentów
  Areny; bez kluczy w runtime, zgodne z ADR 0001–0003), ilustracje
  pregenerowane, przykład dossier (Balor). Cross-link w `LUZNE_POMYSLY...`.
- **C1** `kentaur-pelion`: metopy Partenonu (świadectwo materialne), +2 źródła.
- **C3** SKIT „NIEPROSZENI GOŚCIE” (`#skit:nieproszeni-goscie`): trio Imp ×
  Kentaur × Empusa, metoda embodimentu (prototyp KRONIKI), 229 słów, lekki ton.

## Stan na koniec

- Gałąź `arena/01a048ac-ame`, **PR #6 otwarty**. `npm test` **144/144**;
  build/check zielone; indeks deterministyczny: **8 wpisów, 9 SKITów**, 26
  pozycji feedu, 26 tagów. Protokół v1.7. Podpowiedź składów: 5 duetów vs 4 ≥3.
- Brak zmian w kodzie aplikacji w tej sesji (poza treścią i dokumentami);
  `app/arena.js` bez zmian (fundament z M9 stoi, nadal poza UI).

## Co się zmieniło (pliki, względem M9)

| Plik | Zmiana |
|---|---|
| `docs/plans/POMYSL_splot-i-kronika-koncepcja.md` | **nowy** — pogłębiona koncepcja A+B (embodiment) |
| `docs/LUZNE_POMYSLY_NA_PRZYSZLOSC.md` | cross-link „Rozwinięcie koncepcji (M10)” |
| `data/manifestations/kentaur-pelion.json` | C1: metopy Partenonu, +2 źródła, meta |
| `data/skity/nieproszeni-goscie.json` | **nowy SKIT** (trio, embodiment) |
| `docs/plans/PLAN_2026-08-28-m10-...` | plan sesji |
| `docs/ROADMAP.md`, `docs/PROJECT_HISTORY.md` | sekcja M10 |

## Rzeczy otwarte / kolejka — DECYZJE WŁAŚCICIELA

1. **Kierunek grywalizacji** (pytania na końcu
   `docs/plans/POMYSL_splot-i-kronika-koncepcja.md`): (a) rola gracza w SPLOCIE
   — obserwator / kustosz-reżyser / oba? (b) architektura AI — start od 3b (bez
   kluczy), 3a później? (c) czy „Kronika jako dane w repo, popychane sesjami” to
   kierunek, pod który napisać ADR?
2. **Scalenie PR #6** (M7–M10).
3. **Pętla Jakości** dalej: C1 (najsłabsze wpisy mają teraz 5–6 źródeł — balor
   ma 2 powiązania i 0 modyfikacji), C3 (celuj w 3–4 osoby; build podpowiada),
   rozbudowa kartoteki do ~15–20 wpisów (fundament pod każdą grę).
4. Od następnej zmiany treści: `meta.modyfikacje[].data` z godziną.

## Pułapki tej sesji

- **Heredok z pythonem a polskie znaki w kodzie:** literał z myślnikiem „—”
  (U+2014) w źródle pythona wywalił parser (`SyntaxError: invalid character`).
  Reguła: tekst z UTF-8 wczytuj z pliku (`/tmp/*.txt`), nie wklejaj do kodu
  źródłowego heredoka (rozwinięcie L2).
- **Namysł to praca C2** — właściciel poprosił o myślenie, nie kod; deliverable
  tej rundy to dokument koncepcyjny + treść, nie feature w UI.
