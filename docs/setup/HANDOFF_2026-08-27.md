# HANDOFF — sesja 2026-08-27 (M1: fundament F0)

> Skrót JEDNEJ sesji. Zasady żyją w `AGENTS.md`, `docs/PROTOKOL.md` i ADR —
> w razie rozbieżności wygrywa repozytorium.

## Stan na koniec sesji

- Gałąź: `arena/01a04525-ame`, PR: https://github.com/szybkoiwyraznie-rgb/AME/pull/1
  (6 commitów: zasady → narzędzia → dane → aplikacja → wizualizacje → CI/handoff).
- `npm test` — **32 testy zielone** (schemat wpisów, indeks, geo/projekcja,
  dekoder TopoJSON, realny asset mapy, generator HTML panelu).
- `npm run build` — przebudowuje `data/index.json` (2 wpisy, 10 tagów).
- Aplikacja działa w całości offline: mapa wektorowa Natural Earth,
  pan/zoom (drag/kółko/pinch/dwuklik/przyciski), pinezki, panel wpisu
  (sekcje I–V + powiązania/backlinki + prompt z kopiowaniem), szukanie,
  tagi, lista kartoteki, łuki powiązań, hash-routing `#slug`.
- Wpisy: `wendigo` (Tarmogoyf) i `zmora` (Nightmare) — pełne kartoteki
  wg protokołu MFM v1.1, z obrazami 21:9.

## Jak uruchomić

```bash
npm test && npm run build
python3 -m http.server 8000 --bind 0.0.0.0   # http://localhost:8000
```

`file://` pokaże baner z instrukcją (ograniczenie przeglądarki, nie błąd).

## Oczekiwane wyniki po scaleniu

- `npm test`: 32 pass / 0 fail; `npm run build`: „2 wpisów, 10 tagów”.
- CI po wklejeniu przepisu (patrz kolejka) zielone na PR i na `main`.
- GitHub Pages: Settings → Pages → Deploy from a branch → `main`, `/ (root)`
  → `https://<owner>.github.io/AME/` (do wykonania przez właściciela).

## Rzeczy otwarte / kolejka

1. **Właściciel:** scalić PR #1 (preferowane *Squash and merge*).
2. **Właściciel:** po scaleniu skopiować `docs/setup/ci-workflow.yml`
   → `.github/workflows/ci.yml` (token agenta nie ma uprawnień `workflows`).
3. **Właściciel:** włączyć GitHub Pages (branch `main`, folder `/`).
3. **Następna sesja:** audyt PR #1 wg ADR 0004, potem F1 — proces treściowy:
   nowe karty od właściciela → wpisy, rozbudowa opisów, tagowanie, powiązania,
   wizualizacje dla wpisów bez obrazu (`docs/ROADMAP.md`).

## Pułapki (pełna lista: `docs/setup/ENVIRONMENT.md`, `docs/LESSONS.md`)

- Sandbox resetuje workspace — pushuj po każdym zielonym kroku; odzyskiwanie
  wg ENVIRONMENT §2.
- `edit_file` psuje polskie znaki — poprawki przez python3/UTF-8.
- Egress zablokowany (npm i api.github.com działają); dane z sieci przez
  narzędzia agenta.
- Nie edytować `data/index.json` ręcznie — tylko `npm run build`.
- node 22: `node --test` bez argumentu (katalog jako argument nie działa).
