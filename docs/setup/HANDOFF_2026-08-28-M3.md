# HANDOFF — sesja 2026-08-28 M3 (poprawki z testów + Baza Skitów + „Co nowego”)

> Skrót JEDNEJ sesji. Zasady żyją w `AGENTS.md`, `docs/PROTOKOL.md` i ADR —
> w razie rozbieżności wygrywa repozytorium. Poprzedni skrót (M2):
> `HANDOFF_2026-08-28.md`.

## Stan na koniec sesji

- Gałąź: `arena/01a0477a-ame`, **PR #2** (otwarty, opis kumulatywny).
- `npm test` — **94/94 zielone**; `npm run build` — **4 wpisy, 2 SKITy, 8 pozycji
  feedu, 22 tagi**; `npm run check` — indeks spójny.
- **Audyt PR #1** (pliki + brama CI) w `docs/PROJECT_HISTORY.md` (M3). Znalezione
  i naprawione: artefakt antypołudnika, model widoku mapy, jednostki pinezek,
  luki testów. Znalezione i zamknięte jako fakt (bez próśb — L12): `.github/workflows/ci.yml`
  na `main` jest nieprawidłowym YAML (spuścizna przepisu F0), więc CI nie uruchamia
  jobs; agent nie ma uprawnień `workflows` (403 przy push i API), a jednorazowa
  czynność właściciela została wykonana 2026-08-27. Bramą sesji pozostają lokalne
  testy i build indeksu; treść workflow czeka w `docs/setup/ci-workflow.yml`.
- **Zrealizowane zgłoszenia testera:** A1 proporcje (widok w px kontenera),
  A2 motyw jasny/ciemny (przełącznik w topbarze, tokeny CSS, localStorage),
  A3 cięcie na antypołudniku, A4 badge 19 px, A5 większa pinezka + pole trafienia,
  B1 kolejność i NUMERACJA sekcji (v1.3), B2 pełnoekranowa warstwa wpisu,
  B3 `dokumentacja[].url` z adresami zweryfikowanymi w sieci.
- **Zrealizowane zlecenia (popołudniowe):**
  - **Baza Skitów** (ADR 0013, PROTOKÓŁ §8): `data/skity/*.json`, walidator
    (2–4 uczestników, unikalny skład, 60–300 słów (po ADR 0015), brak żargonu gry), indeks v2
    (`skity[]`, `manifestacje[].skity`, `aktualizacje[]`), przycisk „✎ skity”,
    widok skitu z deep-linkiem `#skit:<slug>`, sekcja VI pod kartą bytu.
    Pierwszy SKIT: **„PŁÓTNO I KAMIEŃ”** (Egungun × Imp z Lincoln, 241 słów).
  - **C3 w Pętli Jakości** (AGENTS §2, PROTOKÓŁ §8.4) — kolejność: C1 → C3 → C2.
  - **„Co nowego”** (ADR 0014, PROTOKÓŁ §9): przycisk „✚ nowości”, feed liczony
    z `meta` (dodano/zmieniono), najnowsze na górze, pozycje linkują do treści.
    Rygor: KAŻDA zmiana treści = wpis w `meta.modyfikacje` z datą commita.
- **Nowe dokumenty:** ADR 0009–0014, PROTOKÓŁ v1.3, lekcje L7–L10,
  `docs/plans/PLAN_2026-08-28-poprawki-z-testow.md`.

## Runda poprawek po teście właściciela (2026-08-28, popołudnie)

- **Feed „Co nowego" miał odwrotną kolejność** — naprawione w generatorze: sort
  `data desc → sekwencja zdarzenia w pliku desc → typ → slug` (L11). Nie używaj
  rangy typu akcji jako drugiego klucza.
- **Karta bytu była pełna notatek roboczych** — stopka renderuje teraz wyłącznie
  `utworzono` + `zmieniono` (`ui.js: htmlStopki`); usunięte zdanie „pisze je
  kolejna sesja (PROTOKÓŁ §8)" z sekcji VI; `temat` skitu nie jest już
  pokazywany (pozostaje polem katalogowym w danych), a wiersz bazy mówi
  „skład: … · N słów · data".
- **Opisy w `meta.modyfikacje` obu kart przepisane na język czytelnika** — to
  one są dziś na górze feedu. Zasada (PROTOKÓŁ §9, AGENTS §3, WORKFLOW §10):
  opis = zdanie o treści zmiany, bez `M3`/`B1`/`C1`/`PROTOKÓŁ §`; zmiany
  techniczne idą do `PROJECT_HISTORY` i opisu PR, nie do feedu.

## Jak uruchomić i co klikać

```bash
npm test && npm run build
python3 -m http.server 8000 --bind 0.0.0.0
# /#egungun — pełnoekranowa kartoteka (sekcje I→VI), linki źródłowe, motyw
# /#skity — Baza Skitów; /#skit:plotno-i-kamien — sam SKIT
# /#nowosci — dziennik zmian (feed z meta)
```
Warto sprawdzić na żywo: rozciągnięcie okna (mapa nie skacze, brak ucięcia
biegunów), przełącznik ☾/☀ przy otwartej kartotece i przy otwartym skicie,
klik w uczestnika skitu (wraca do karty), Esc (warstwa → kartoteka).

## Domknięcie dnia: materializacje na zlecenie (M3c)

- **Nowe wpisy (F1):** `balor` ← Weftblade Enhancer (EOE 44, 2025; Tory Island
  55.26, −8.22) i `selkie-sule-skerry` ← Kulrath Mystic (ECL 56, 2026; Sule
  Skerry, Orkney 59.08, −4.41). Obie karty ściągnięte ze Scryfall, każde zdanie
  i każde źródło potwierdzone w sieci z adresem (6 i 4 pozycje), wizualizacje
  21:9 wygenerowane i przycięte (1568×672, ≤ 272 KB).
- **C3:** SKIT „ZNAK I LICZBA” (248 słów, skład balor + selkie-sule-skerry —
  unikalny) → sekcja VI obu kart i pozycja w feedzie. Pierwsze dwa szkice miały
  280 i 251 słów i walidator nie przepuścił budów — limit §8.2 działa.
- **CI:** `.github/workflows/ci.yml` na `main` naprawiony przez właściciela
  (PR #3) i włączony do gałęzi merge’em `37052ef`; w repo zostały receptura
  (`docs/setup/ci-workflow.yml`) i test „przepis CI jest wklejalny”.
- **Słownik tagów urósł do 22** — przy najbliższej sesji warto zaproponować
  właścicielowi kanon tagów (BACKLOG: „Kanon tagów”).

## Rzeczy otwarte / kolejka

1. **Właściciel:** scalenie PR #2 (decyzja o wejściu na live = akceptacja
   nowych widoków: Baza Skitów, sekcja VI, „Co nowego”, motyw, overlay).
2. **CI — zamknięte, bez próśb.** `.github/workflows/ci.yml` na `main` nie parsuje
   się (spuścizna przepisu F0, nie czyjaś zaległość), agent nie ma uprawnień
   `workflows` (403 przy push i `gh api`), a jednorazowa czynność właściciela
   została wykonana 2026-08-27. **Nie odnawiaj tego wątku** w PR-ach, handoffach
   ani listach „otwarte” (AGENTS §4, ENVIRONMENT §3, L12). Bramą sesji są
   `npm test` + `npm run build` + `npm run check`; treść workflow czeka
   w `docs/setup/ci-workflow.yml`, jeśli właściciel sam kiedyś zechce jej użyć.
3. **Następna sesja:** PR + plan → audyt PR #2 → zlecenia → Pętla Jakości
   C1 (pogłębianie wpisów) → **C3 (SKIT o nowym składzie — wymaga nowych
   materializacji: przy 2 kartach istnieje tylko jeden dozwolony skład, więc
   pierwszy krok to F1: opracować kolejne karty)** → C2.

## Pułapki tej sesji (nowe lekcje)

- L7 antypołudnik (cięcia wymagają WSZYSTKIE geometrie kuliste, nie „Rosja”),
  L8 jednostki w SVG (px w grupie skalowanej ≠ px na ekranie),
  L9 audyt bramy: `gh run list`, nie tylko `git diff`,
  L10 delegacja klików: `closest()` z listą atrybutów (rodzic pożerał dziecko).
- Lekcja L9 (sprawdzanie, czy brama działa) służy **opisowi stanu**, nie stawianiu żądań — patrz L12.
- Testy renderu bez przeglądarki: `test/pinezka.test.js` (atrapa DOM) i
  jednorazowy smoke na jsdom (zainstalowanym `--no-save`) — JSDOM nie wspiera
  `<script type=module>`, więc `app.js` importuje się ręcznie zglobalsami.
