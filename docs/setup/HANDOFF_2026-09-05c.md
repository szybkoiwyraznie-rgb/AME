# HANDOFF — 2026-09-05c (obroty 2 i 3 Pętli Jakości, PR #34)

Poprzedni handoff: `docs/setup/HANDOFF_2026-09-05b.md` (obrót 1 + ADR 0025 + sprzężenie Kronika ↔ SPLOT).

## Stan na koniec sesji

- Gałąź: `arena/01a0712f-ame`, PR **#34** (base `main`, otwarty), HEAD `53c7816` — wypchnięty.
- `npm test` → **285 pass / 0 fail**; `npm run build` → 0; `npm run check` → 0 (rebuild, kronika, lint stylu, **lint źródeł**).
- Kartoteka: **25 wpisów**, **46 SKIT-ów**, **2 drogi SPLOTU**, Kronika Tomu I = 11 Epok, kanon = **56 tagów** (wszystkie użyte).
- Budżet lektury startowej: 34 423 tok = **86,1 %** progu 40 000 (`node tools/budzet-lektury.mjs`) — przy kolejnych ADR-ach warto pilnować.

## Co zrobiono w tej sesji

### Obrót 2 (commity `3163693` → `b163dc6`)

| krok | rzecz |
| --- | --- |
| C4 | `bazyliszek-warszawski` — karta Coat with Venom (DTK #91, 2015), Krzywe Koło, źródła: Słownik polskiej bajki ludowej UMK, Zmorski 1852 (Wikiźródła), Oppman 1925, Smithsonian (Pincier 1605); tagi `krol-wezy`, `jad` |
| C1 | `syama-i-sarvara` — krąg *sārameya* z Rigwedy 7.55 (usypianie psa strażnika), jawne odrzucenie etymologii Śarvara ↔ Kerberos (Lincoln 1977 vs Beekes), powiązanie z morową panną |
| C3 | SKIT `lustro-odpowiedz-wers` (bazyliszek + morowa panna + psy Yamy), 271 słów |
| C6 | **PRÓG** — `app/prog.js`, ADR **0026**, widok `#prog`, przycisk „🔑 próg”, `test/prog.test.js` (10 testów) |
| C2+5 | **lint źródeł** — `tools/lint-zrodel.mjs` (reguły Z1–Z5) w `npm run check`, `test/lint-zrodel.test.js` |

### Obrót 3 (commity `d5b6d55` → `53c7816`)

| krok | rzecz |
| --- | --- |
| C4 | `glamr-z-thorhallsstadir` — karta Banishment Decree (MBS #3, 2011, „Unworthy of consecration”), Grettis saga rozdz. 32–35, Minjastofnun Íslands (Glámsþúfa), współrzędne 65.31 / −20.1328; **pierwsza kultura islandzka**, tagi `islandia`, `powracajacy-zmarly`, `klatwa` |
| C1 | `neith-z-sais` — list Neith z Papirusu Chester Beatty I (wyrok + rekompensata dla Seta) i archeologia Sais (EES/Durham, Penelope Wilson; len w warstwach Nowego Państwa), 3 nowe źródła równoważące Wikipedię |
| C3 | SKIT `rachunek-za-pochowek` (Egungun + Glámr + Neith), 252 słowa |
| C6 | **katalog dróg SPLOTU**: `wczytajDrogi`/`walidujDroge`/`rekordyDrog` w `tools/rebuild-index.mjs`, pole `drogi` w indeksie, pasek wyboru drogi w UI, druga droga `popiol-z-forsaeludalur` + grafika; `test/splot-katalog.test.js` |
| C2+5 | **miniatury na pinezkach** od zoomu ×6 (`PROGI_WARSTW.miniatury`, `clipPath #przyciecie-pinezki`, leniwy `href`), backlog odhaczony |

## Nowe narzędzie fabularne: PRÓG (ADR 0026)

`app/prog.js` — spotkanie rozstrzygane kluczem, nie walką:

- `KLUCZE_PROGU` (zwierciadło, odpowiedź, hymn, opłata, ogień, imię, zasłona) — każdy klucz celuje w motywy kanonu; test pilnuje, by żaden nie wskazywał motywu spoza `data/kanon-tagow.json`.
- `kluczeDlaBytu`, `oporProgu`, `szansaProgu` (deterministyczne), `rozstrzygnijProbe` (los, trzy wyniki: `przejscie` / `cena` / `odmowa`), `rozegrajProg`, `tablicaProgow`.
- `pogodaProgu(kronika, slug)` — sprzężenie **jednokierunkowe**: świat mityczny pomaga gestom, zasięg bytu **szkodzi** (odwrotnie niż w SPLOCIE). PRÓG nic nie zapisuje do Kroniki, więc Epoka nadal ma dokładnie jedno źródło (`skit` XOR `zrodloSplotu`).
- **Uwaga przy nowym motywie w kanonie:** trzeba mu dać wagę w `WAGA_MOTYWU` (`app/arena.js`) i rozważyć przypisanie do klucza PROGU — inaczej byt broni się tylko „zasłoną”.

## Reguły operacyjne (bez zmian, potwierdzone w praktyce)

- Nowa karta bytu → `npm run build` → `npm test` **prawie zawsze** wywraca `test/kronika.test.js` (paliwo, oś, zasięgi). Rekalibracja w tym samym kroku (L20); wartości bierze się z `node -e` na `data/kronika/summary.json`, nie z głowy.
- Testy bez potoku (`npm test > /tmp/t.log; grep -E "^# (pass|fail)"`) — L19.
- Commit tylko na gałęzi z otwartym PR (L21), `git commit -F /tmp/msg.txt`, brak force push.
- `app/` bez API Node (L22). Polskie znaki edytować przez `python3` + utf-8.
- `gh pr edit --body-file` **nie działa** (deprecation Projects) → `gh api -X PATCH repos/:owner/:repo/pulls/34 --input plik.json`.
- Cache-bust po zmianach frontu: dziś `c5-24` (CSS) i `c6-9` (moduły JS) — bump w `index.html` **oraz** w importach `app/*.js` (pilnuje `test/ui.test.js`).

## Następny obrót (4) — propozycja

1. **C4** — losowanie z puli nieużytych kart ziarnem `arena/01a0712f-ame#obrot4` (skrypt FNV-1a + xorshift, patrz historia sesji); pula ≈ 463 pozycji. Kultury nadal niedoreprezentowane: Ameryki, Chiny, Azja Południowo-Wschodnia, Bałkany poza Albanią.
2. **C1** — wpis inny niż `neith-z-sais`; najcieńsze karty: `cormoran-st-michaels-mount`, `loup-garou-gevaudan`, `bazyliszek-warszawski`.
3. **C3** — skład unikalny w całej bazie (46 SKIT-ów; sprawdzać `node -e` po `data/skity`).
4. **C6** — narzędzie inne niż PRÓG i niż katalog dróg: np. Epoka XII Tomu I oparta o `rachunek-za-pochowek` albo `zrodloSplotu` z drogi `popiol-z-forsaeludalur` (walidator wymusza zgodność kierunku osi), albo rozwinięcie PROGU o zapis prób.
5. **C2+5** — nowa pozycja z backlogu; otwarte i konkretne: klastrowanie pinezek, warstwa kręgów kulturowych, sekcja III jako numerowane przypisy w karcie, sprawdzanie dostępności linków (wymaga CI poza sandboxem).

## Do decyzji właściciela

- PRÓG i katalog dróg są wpięte w UI (przyciski „🔑 próg”, pasek wyboru drogi) — jak wszystkie poprzednie widoki C2 czekają na akceptację przed live.
- Budżet lektury startowej przekroczył 85 %; przy kolejnych ADR-ach warto albo podnieść próg świadomie, albo skrócić najstarsze ADR-y do streszczeń.
