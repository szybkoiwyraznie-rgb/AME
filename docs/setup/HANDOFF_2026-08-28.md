# HANDOFF — sesja 2026-08-28 (M2: Pętla Jakości + materializacja dwóch kart)

> **Stan zastąpiony:** ta sesja została scalona (PR #1), a dalsze prace opisuje
> `docs/setup/HANDOFF_2026-08-28-M3.md`. Poniżej — wyłącznie historia.

> Skrót JEDNEJ sesji. Zasady żyją w `AGENTS.md`, `docs/PROTOKOL.md` i ADR —
> w razie rozbieżności wygrywa repozytorium.

## Stan na koniec sesji

- Gałąź: `arena/01a04525-ame`, PR #1 (otwarty, aktualizowany kumulatywnie),
  commity 1047089..607487a na tej sesję.
- `npm test` — **34/34 zielone**; `npm run build` — 2 wpisy, 10 tagów.
- Na `main` (poza PR): `.github/workflows/ci.yml` i GitHub Pages dodane
  przez właściciela 2026-08-28 (status: aktywne; Pages:
  https://szybkoiwyraznie-rgb.github.io/AME/ ).
- **Nowe zasady tej sesji:** ADR 0007 (Pętla Jakości: po audycie, bez
  zlecenia → rekurencyjne C1 treść ↔ C2 featury do wyczerpania budżetu;
  polerowanie dokumentacji tylko na zlecenie właściciela) i ADR 0008
  (obowiązkowy fetch Scryfall dla każdej karty; obowiązkowa weryfikacja www
  każdego wpisu i każdej pozycji bibliografii; zakaz pisania z pamięci).
- **Dane:** wendigo i zmora USUNIĘTE (zlecono). Nowe pełne materializacje:
  - `egungun` ← Krumar Initiate (TDM 84; Scryfall: {1}{B} 2/2 Human Cleric,
    „{X}{B},{T},Pay X life: endures X”, flavor: duchy rodu Emesh witają
    Cemala w Kin-Tree; art. Josu Solano, watermark Abzan). Transpozycja:
    jorubski kult przodków egúngún w Oyo (ara ọ̀run, inicjacja, Alapini,
    oríkì, warstwy płócien). Źródła: Drewal–Pemberton 1989; badania Òyó
    (2019); ModernGhana.
  - `lincoln-imp` ← Forge Devil (DKA 91; Scryfall: {R} 1/1 Devil,
    obowiązkowe ETB 1 dmg kreaturze + 1 graczowi; flavor o diabłach
    pękających filary katedry Thraben; art. Austin Hsu). Transpozycja: legenda
    o impie z katedry Lincoln (Angel Choir, anioł z hymnalu, skamienienie,
    wariant Grimsby). Źródła: BBC Lincolnshire; BBC News (16.11.2025);
    materiały katedry Lincoln.
  - Powiązanie: lincoln-imp → egungun (lustrzane reguły sacrum); backlink
    automatyczny. Wizualizacje 21:9 wygenerowane i podpięte (≤ 2 MB).
- Dokumentacja: `docs/WORKFLOW.md` zawiera pełną procedurę dodawania
  manifestacji krok po kroku (fetch Scryfall → research → wpis → build →
  ilustracja → podłączenie do mapy przez indeks).

## Jak uruchomić

```bash
npm test && npm run build
python3 -m http.server 8000 --bind 0.0.0.0   # http://localhost:8000
# deep-linki: /#egungun, /#lincoln-imp
```

## Oczekiwane wyniki po scaleniu

- `npm test`: 34 pass / 0 fail; build: „2 wpisów, 10 tagów”.
- CI zielone (workflow na main od właściciela).
- Po scaleniu PR #1: Pages pokaże nowe wpisy (deploy z main).

## Rzeczy otwarte / kolejka

1. **Właściciel:** decyzja o scaleniu PR #1 (Squash and merge).
2. Po scaleniu: ewentualne usunięcie `docs/setup/ci-workflow.yml` (receptura
   wciąż aktualna historycznie; ci.yml już żyje na main).
3. **Następna sesja** (wg ADR 0007): PR + plan → audyt PR #1 → zlecenia
   właściciela (nowe karty) → Pętla Jakości C1/C2 do wyczerpania budżetu.

## Pułapki tej sesji (nowe lekcje L5/L6)

- Po odzyskaniu workspace (reset między turami) pierwszy push wymaga `-u`
  (upstream znika); stan zdalny sprawdzaj `git ls-remote`.
- Reset sandboxa ściera pakiety pip (pillow!) — skrypty obróbki uruchamiać
  osobno, czytać STDERR, weryfikować efekt przed commitem.
- Pełna lista: `docs/setup/ENVIRONMENT.md`, `docs/LESSONS.md`.
