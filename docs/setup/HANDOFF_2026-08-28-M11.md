# HANDOFF — sesja 2026-08-28 M11 (obserwator + realia AI + timeline'y + treść)

> Skrót JEDNEJ sesji. Zasady: `AGENTS.md`, `docs/PROTOKOL.md` (v1.7), ADR 0001–0019.
> Poprzednie: `HANDOFF_2026-08-28-M10.md` … `-M3.md`, `HANDOFF_2026-08-28.md` (M2),
> `HANDOFF_2026-08-27.md` (M1).

## Kontekst

Właściciel na 3 pytania z M10: (1) rola OBSERWATORA ciekawsza; (2) **wątpliwość:
czy agent Areny może powoływać subagentów?** (żył w przekonaniu, że to jeden
wszechwiedzący agent); (3) zależy od (2). Plus nowy pomysł: fabuła jako
niezależne timeline'y. Na razie MYŚLIMY, nie kodujemy gry; treść w międzyczasie.

## Co zrobiono

- **Uczciwe sprostowanie o AI** (`POMYSL_splot-i-kronika-koncepcja.md` §1a + §2.4):
  agent Areny to jeden proces w turze — NIE powołuje niezależnych subagentów.
  „Subagenci-postacie" = metafora metody. Trzy realne opcje: symulowany
  wielogłos „blind-draft" (dziś), izolacja przez kolejne sesje (wolna), pełna
  izolacja = 3a (klucze, osobny ADR). To ważne: obietnica pełnej izolacji bez
  kluczy była nierealna — teraz nazwana wprost.
- **Rola gracza = OBSERWATOR** (§3, decyzja właściciela) — domyślny tryb SPLOTU.
- **Pomysł (11)** w `LUZNE_POMYSLY`: fabuła jako niezależne timeline'y
  (rozszczepienie/dead-end/krzyżowanie) — Kronika jako graf wątków.
- **C1** `balor`: oś proroctwa (Ethniu/Tor Mór/Loch na Súl), +1 źródło,
  obustronne powiązania z barbarossa i drangue (sieć balora domknięta).
- **C3** SKIT „TRZY ZEGARY” (`#skit:trzy-zegary`): trio Barbarossa × Balor ×
  Drangue, pierwszy pisany dyscypliną blind-draft (§2.4).

## Stan na koniec

- Gałąź `arena/01a048ac-ame`, **PR #6 otwarty**. `npm test` **144/144**;
  build/check zielone; indeks: **8 wpisów, 10 SKITów**, 28 pozycji feedu, 26
  tagów. Protokół v1.7. Składy wieloosobowe: **5 vs 5 duetów** (równowaga).
- Kod aplikacji bez zmian w tej sesji (`app/arena.js` z M9 stoi, poza UI).

## Co się zmieniło (pliki, względem M10)

| Plik | Zmiana |
|---|---|
| `docs/plans/POMYSL_splot-i-kronika-koncepcja.md` | §1a (realia AI), §2.4 (blind-draft), §3 (obserwator), §6/§9 zaktualizowane |
| `docs/LUZNE_POMYSLY_NA_PRZYSZLOSC.md` | pomysł (11) timeline'y |
| `data/manifestations/balor.json` | C1: oś proroctwa, +1 źródło, +2 powiązania, meta |
| `data/skity/trzy-zegary.json` | **nowy SKIT** (trio, blind-draft) |
| `docs/plans/PLAN_2026-08-28-m11-...` | plan sesji |
| `docs/ROADMAP.md`, `docs/PROJECT_HISTORY.md` | sekcja M11 |

## Rzeczy otwarte / kolejka — DECYZJE WŁAŚCICIELA

1. **Architektura AI po sprostowaniu (§1a):** czy akceptujesz, że pełna izolacja
   bytów = dopiero 3a (klucze w pamięci sesji, osobny ADR łamiący ADR 0003), a do
   tego czasu prototypujemy na symulacji (blind-draft + izolacja przez sesje)?
2. **Czy pisać ADR „Kronika jako dane w repo, popychane sesjami"** + plan
   pierwszej epoki (tryb obserwatora)? Timeline'y (pomysł 11) mogą wejść od razu
   jako graf wątków.
3. **Scalenie PR #6** (M7–M11).
4. **Pętla Jakości** dalej: rozbudowa kartoteki do ~15–20 wpisów (fundament pod
   grę), C3 wg blind-draft, C1 z researchem.
5. Od następnej zmiany treści: `meta.modyfikacje[].data` z godziną.

## Pułapki tej sesji

- **Nie obiecuj zdolności, których nie masz.** Koncepcja z M10 zakładała
  niezależnych subagentów — a agent Areny ich nie powołuje. Zawsze weryfikuj
  realia własnych narzędzi, zanim wpiszesz je w architekturę (§1a to naprawia).
- **Heredok + polskie znaki w źródle pythona:** teksty UTF-8 (z „—", „…")
  wczytuj z pliku `/tmp/*.txt`, nie wklejaj do literałów w kodzie (L2, M10).
