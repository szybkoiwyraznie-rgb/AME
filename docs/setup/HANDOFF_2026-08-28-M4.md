# HANDOFF — sesja 2026-08-28 M4 (nowa sesja: audyt PR #3 + pierwszy obieg pętli C1→C3→C2)

> Skrót JEDNEJ sesji. Zasady: `AGENTS.md`, `docs/PROTOKOL.md` (v1.4), ADR 0001–0015.
> Poprzednie skróty: `HANDOFF_2026-08-28-M3.md`, `HANDOFF_2026-08-28.md` (M2),
> `HANDOFF_2026-08-27.md` (M1).

## Stan na koniec sesji

- Gałąź: `arena/01a0477a-ame`, **PR #2 otwarty i `MERGEABLE`**, CI na PR-ach
  **zielone** (brama działa od PR #3 właściciela: `npm test` → `npm run build` →
  `npm run check` + `git diff --exit-code data/index.json`).
- `npm test` — **99/99**; `npm run check` — spójny; indeks: **4 wpisy, 3 SKITy,
  10 pozycji feedu, 22 tagi**.
- Zmiany tej sesji:
  1. **Limit SKITa 250 → 300 słów** (zlecenie właściciela): `SKIT_MAX_SLOW`,
     PROTOKÓŁ v1.4 (§4.5, §8.2), AGENTS §3, README/WORKFLOW/ARCHITECTURE/ROADMAP,
     ADR 0015, test kontraktowy „limit długości jest jeden: kod = protokół =
     instrukcja”.
  2. **Audyt PR #3** + nadpisanie dokumentów, które po naprawie `ci.yml`
     głosiły nieaktualny stan („CI się nie parsuje”); `docs/setup/ci-workflow.yml`
     jest teraz **lustrem** pliku z `main`, a test `receptura CI jest zgodna z
     plikiem workflow` pilnuje braku rozjazdu.
  3. **C1**: `selkie-sule-skerry` pogłębiony (tradycja orkadzka i szetlandzka,
     sea-kist, siedem łez, Marwick/Dennison/Hibbert), +2 źródła z adresami,
     opis zmiany w `meta.modyfikacje` → feed 8 → 9.
  4. **C3**: SKIT `hold-oraz-swiatlo` („HOŁD ORAZ ŚWIATŁO”, 276 słów,
     balor × egungun × lincoln-imp) → sekcja VI trzech kart + pozycja w feedzie.
  5. **C2**: „⧉ kopiuj link” w kartotece i w warstwie skitu (`ui.linkWidoku`,
     `ui.przyciskKopiowania`, `app.kopiujLink` z fallbackiem) + test kontraktowy
     handlerów. **C2 czeka na akceptację właściciela** — nie scalać bez decyzji.

## Jak uruchomić i co klikać

```bash
npm test && npm run build
python3 -m http.server 8000 --bind 0.0.0.0
/#egungun            — kartoteka: sekcje I→VI, ⧉ kopiuj link obok ✕
/#skity              — Baza Skitów (3 pozycje)
/#skit:hold-oraz-swiatlo   — trio Balor × Egungun × Imp
/#nowosci            — feed (10 pozycji, najnowsze na górze)
```
Warto sprawdzić na żywo: kopiowanie linku w mapie motywów jasnym/ciemnym,
sekcja VI na wszystkich czterech kartach, fallback przycisku bez Clipboard API.

## Wpis nr 5 i 6 (2026-08-28, zlecenie właściciela)

- **`barbarossa-kyffhaeuser`** ← Alaborn Trooper (P02 #4, 1998): cesarz śpiący w
  Kyffhäuserze (Grimm, Deutsche Sagen 23; pomnik 1890–1896; jaskinia z 1865) —
  Turyngia, 51.41, 11.11; karta bez zdolności czytana jako „byt z terminem”.
- **`drangue-shala`** ← Thunderstaff (DST #153, 2004): albański drangue z doliny
  Shali (Theth, 42.40, 19.77); ochrona kamienia piorunowego = „untapped prevents
  1”, wyniesienie ognia i bicie w metal = +1/+0 do końca burzy. Wpis odnotowuje,
  że ten druk nie ma flavor textu (zdanie o Jorcie pojawia się w nowszych
  przedrukach).
- Obie karty mają wizualizacje 21:9 (1568×672, ≤ 229 KB) i 5–6 źródeł z adresami.
- Test straży: polska proza nie zawiera pisma japońskich/chińskich ani cyrylicy
  (w tej sesji dwukrotnie wkradły się takie znaki).

## Rzeczy otwarte / kolejka

1. **Właściciel:** decyzja o scaleniu PR #2 (w niej: akceptacja C2 =
   kopiowania linku oraz wszystkich widoków M3).
2. **Kanon tagów** (BACKLOG): przy 22 tagach warto zdecydować, które są
   systemowe (kontynent/kultura/typ bytu/pora), a które swobodne — propozycję
   powinien złożyć następny C1.
3. **C3 przy 4 wpisach:** możliwe składy to pary (6, dwa użyte) i tria (4,
   jedno użyte) — kolejne obiegi są dozwolone, ale coraz bardziej kosztują
   oryginalnością tematu; jeśli przybędzie 5.–6. byt (F1), wracają swobodne pary.
4. **Następna sesja:** §0 w całości → PR z planem → audyt (najbliższy scalony PR)
   → zlecenia właściciela → C1 → C3 → C2.

## Pułapki tej sesji

- `indexOf` w testach kontraktowych: jeśli wzorzec występuje wcześniej w pliku,
  wycinek jest pusty i test wyje mylnie (albo przechodzi mylnie) — sprawdzaj
  kolejność wystąpień.
- `npm test | grep` nie jest bramą: commit „po grepu” ogłosił 99/99 przy 98/99
  (sprostowane w `93f9792`). Bramą jest **kod wyjścia** `npm test`.
- Komunikaty commitów: przez `git commit -F -`/plik, bez backticków w `bash -c`,
  i bez wtrąceń obcosłownych (zdarzyło się jedno).
