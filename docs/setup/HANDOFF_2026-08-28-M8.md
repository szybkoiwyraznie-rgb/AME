# HANDOFF — sesja 2026-08-28 M8 (zlecenia A/B/C + Pętla Jakości)

> Skrót JEDNEJ sesji. Zasady: `AGENTS.md`, `docs/PROTOKOL.md` (v1.7), ADR 0001–0019.
> Poprzednie: `HANDOFF_2026-08-28-M7.md`, `-M6.md`, `-M5.md`, `-M4.md`, `-M3.md`,
> `HANDOFF_2026-08-28.md` (M2), `HANDOFF_2026-08-27.md` (M1).

## Kontekst

Właściciel **zaakceptował** featurę „🎲 wylosuj" z M7 („Biorę") i zlecił trzy
kwestie (A/B/C), a potem kolejną sesję jakości. PR #6 był wciąż otwarty, więc
całość dołożona jako kolejne commity tej samej gałęzi/PR (sesja przypięta do
`arena/01a048ac-ame`).

## Zlecenia właściciela

- **A — SKITy 3–4-osobowe preferowane** (ADR 0019, PROTOKÓŁ v1.7 §8.2):
  stała `SKIT_ZALECANE_UCZESTNIKOW=3`, czysta `podpowiedzSkladySkitow` +
  niełamiąca podpowiedź w `npm run build`. Minimum 2 zostaje (duety legalne).
- **B — opis powiązania jako zdanie** (ADR 0019, §6.2): `walidujPowiazania`
  odrzuca opis <12 słów (`POWIAZANIE_MIN_SLOW`) i goły `http(s)`-link.
  Istniejące opisy (43–78 słów) przechodzą; próg pilnuje przyszłości.
- **C — grywalizacja (idle battler)**: projekt + zręby (nie pełny kod).
  `docs/plans/POMYSL_arena-rezonansu-idle-battler.md` + `app/arena.js` +
  `test/arena.test.js`. Staty z rekordu indeksu, deterministycznie. **ZERO UI.**

## Pętla Jakości (ADR 0007)

- **C1** `lincoln-imp`: research www (dwaj impowie, anioł z hymnału, Grimsby,
  wiatr krążący za bliźniakiem) + 2 źródła + obustronne powiązania (balor,
  drangue). `meta` z godziną.
- **C3** SKIT „KALENDARZ POWROTÓW" (`#skit:kalendarz-powrotow`): trio Barbarossa
  × Egungun × Selkie — skład 3-osobowy (demonstracja A), 252 słowa, ciepły ton.
- **C2** = realizacja zlecenia C (patrz wyżej).

## Stan na koniec

- Gałąź `arena/01a048ac-ame`, **PR #6 otwarty**. Commity M8: plan → A+B (ADR 0019,
  v1.7) → C1 (lincoln-imp) → C3 (trio) → C2 (arena zręby) → dokumenty sesji.
- `npm test` **139/139**; `npm run build` + `npm run check` zielone, indeks
  deterministyczny: **8 wpisów, 7 SKITów**, 22 pozycje feedu, 26 tagów.
  Protokół **v1.7** (ADR 0019).
- CI na PR: zielone (sprawdzane `gh run list`).

## Co się zmieniło (pliki, względem M7)

| Plik | Zmiana |
|---|---|
| `tools/rebuild-index.mjs` | stałe `SKIT_ZALECANE_UCZESTNIKOW`, `POWIAZANIE_MIN_SLOW`; `walidujPowiazania` (opis <12 słów / sam link); `podpowiedzSkladySkitow` + hint w buildzie |
| `docs/decisions/0019-*.md` | **nowy ADR** (skład 3–4, opis powiązania), rejestr README |
| `docs/PROTOKOL.md` | v1.7: §6.2 (opis powiązania), §8.2 (skład preferowany), §8.4 |
| `AGENTS.md`, `docs/WORKFLOW.md`, `index.html`, `README.md` | v1.7 + reguły A/B |
| `data/manifestations/lincoln-imp.json` | C1: pogłębienie, +2 źródła, +2 powiązania, meta |
| `data/skity/kalendarz-powrotow.json` | **nowy SKIT** (trio) |
| `app/arena.js` | **nowy moduł** — zręby idle battlera (czyste funkcje) |
| `test/arena.test.js` | **nowy** (10 testów); `schema.test.js`, `skit.test.js` +kontrakty A/B |
| `test/rebuild-index.test.js`, `test/fixtures/katalog-ok/zorza.json` | opisy powiązań ≥12 słów |
| `docs/plans/POMYSL_arena-rezonansu-idle-battler.md`, `PLAN_...-m8-...md` | projekt + plan |
| `docs/ROADMAP.md`, `docs/PROJECT_HISTORY.md`, `docs/BACKLOG.md` | sekcja M8 |

## Rzeczy otwarte / kolejka

1. **Właściciel:** (a) scalenie PR #6; (b) **decyzja o „Arenie Rezonansu"** —
   czy budować widok, i w jakim zakresie: osobny `#arena` (rekomendacja) vs
   profil bojowy w kartotece; ton (Rezonans/spór tradycji vs walka). Wpięcie do
   UI dopiero po tej decyzji (ADR 0007).
2. **Pętla Jakości** dalej: C1 (research; kandydaci — empusa/kentaur mają po 3
   źródła), C3 (celuj w 3–4 osoby — build podpowiada, że duetów jest 5:2), C2.
3. Od następnej zmiany treści: `meta.modyfikacje[].data` z godziną.
4. Headless Chromium do weryfikacji featur: `ENVIRONMENT §4.1`.

## Pułapki tej sesji

- **Twardszy walidator łapie własne fixture'y** — po dodaniu progu 12 słów na
  opis powiązania padły dwa testy na danych testowych (`katalog-ok/zorza.json`,
  `rebuild-index.test.js`); to poprawny sygnał, nie fałszywy alarm. Przy
  zaostrzaniu reguły przejrzyj też fixtures.
- **Klucze motywów muszą pasować do kanonu** — w `WAGA_MOTYWU` użyłem `oplata`
  zamiast kanonicznego `oplaty` i pominąłem `burza`; złapał to nowy test
  kontraktowy „każdy motyw kanonu ma wagę". Słowniki gry sprawdzaj z
  `data/kanon-tagow.json`.
